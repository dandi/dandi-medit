name = "dandi-medit-cors-proxy"
main = "worker.js"
compatibility_date = "2026-09-01"

# Origins the proxy will serve, comma separated. Requests from any other
# origin, or with no Origin header, get a 403.
[vars]
ALLOWED_ORIGINS = "https://medit.dandiarchive.org,http://localhost:5173"
